rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC ACCESS FOR MIGRATION (Not secure for production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
